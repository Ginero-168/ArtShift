/**
 * Canonical GPT Image 2 spend ceiling used by server routing and client preflight.
 * The browser may display this estimate, but the server remains authoritative.
 */
export const GPT_IMAGE_2_MAX_COST_USD = 0.05 as const;
